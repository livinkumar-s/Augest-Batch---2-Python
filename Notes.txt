>> Python is a high-level, general-purpose, interpreted programming language.

Human Written (print....)
|
|(Compilation)(in 1 go)
|
Byte code (common) 00111
|
|(interpretation)(translation)
|
Machine Code (computer can understand)

IDE --> Integrated Development Environment (software).vs code, sublime, pycharm...

code ---> execute --> output
CLI --> terminal (ps, cmd, bash...)
extension --> .py
Command --> python filename.py

print 
---------
>> it is used to show a data at the output.
Ex: print(44)

input 
--------
>> prompt input from user.
>> it always returns String.
Ex: input(explStr)

Syntax 
-------
>> grammer of python.
>> It is case sensitive(a is not A)
>> Indendation matters.(whitespace before lines of code.. to define a block of code)

Comments (it is for coders)
---------
>> Comments are ignored by python
>> readable
single line comment --> #
multi line comments --> ''' '''

Variables 
-------------
>> It is a named memory that can contain a data.(Dynamically Typed)
name=data

Naming Rules 
--------------
>> can contain letters, numbers, _.
>> cannot start with numbers.
>> no reserved keywords.

Datatypes 
-----------
to find datatype of an object --> type(obj)
integer --> whole numbers. 1,2,30,-1,-2.
float --> decimal numbers. 0.21323,3.14...


string --> sequence of character. '',"".
Operations:
Concatination --> +
repetition --> *

>> Immutable 
>> Indexed/Ordered

Inbuilt Methods 
----------------
.upper()
.lower()
.title()
.capitalize()

.find(subStr) -1 --> not found
.count(subStr) 

.split(sepChar) --> Str --> List
.join(seq) --> seq --> str


.startswith("subStr")
.endswith(subStr)
.isalpha()
.isdigit()
.isalnum()

Formatted Strings --> f" {var} "

Escape Characters
-------------------
\n --> new line
\t --> tab

boolean --> True/False

Mutable(no reuse) --> List, Dict, Set.
Immutable --> Int, Float, Boolean, Str,Tuple, FrozenSet. (reuse)



Type casting
-------------
str(obj) --> 12 --> "12"
int(obj)
float(obj)
bool(obj)
list()
tuple()
set()
dict()

Operator 
-----------
>> Symbol or keyword that performs an operation on values.
2+2=4

Arithmetic 
------------
+ --> Addition 
- 
*
/
// --> Floor Division 
% --> Modulus
** --> Expo

BODMAS 

Comparison (Boolean)
--------------
== equal 
!=
<
>
<=
>=

Logical (combine 2 cond(Bool))
-------------------------
and --> True --> Both are True. Otherwise --> False
or --> True --> anyone or both are True. False --> Both are False
not --> inverts a boolean.

Assignment  
-------------
= assign
+= add and assign
-+ sub and assign
*= mul and assign
/= div and assign
//= floor div and assign
%= mod and assign
**= expo and assign

Bitwise 
------------
& --> and --> Ex: 4&3
| --> or
~ --> not (~n) (~n=-(n+1))
^ --> x-or Ex: a^b (1 --> not equal, 0 --> equal)
>> --> right shift  num<<pos 
<< --> left shift 

Membership 
--------------
in, not in

Identity 
-------------
is, is not


Iterable Objects 
-----------------
>> an object that can return its members 1 by 1.
Ex: String, List, Tuple, Set, Dict, Range...

Sequence 
-----------
len(obj)---> length of sequence.
>> Ordered Collection of data.
Ex: String, List, Tuple, Range.....

Indexing and Slicing 
---------------------
index number starts from --> 0
negative indexing starts from --> -1
obj[ind]

Slicing 
--------
>> Extracting part of an element.
seq[start:]
seq[:end]
seq[start:end]
seq[start:end:step]
seq[::-1] (reversing)

Control Flow Statements 
-------------------------
Conditional Clause 
---------------------
>> making python to take decision.
if 
---
syntax:
----------
if condition:
    block of code

True --> execute the block. False --> skip it.

Elif 
------
elif condition:
    block of code

True --> execute the block. False --> skip it.

Else:
------
else:
    block of code

False -->  execute the block. True --> skip it

Nested If 
-----------
if cond1: 
    if cond2: 
        block of code

Loops 
--------
>> it executes a block of code for multiple times.

while (when no of iteration is not known)
---------
>> Condition based loop 
syntax
--------
while condition:
    block of code

>> as long as the condition is True it is going to execute the block of code over and over again.

for (no of iteration is known, iterate over a iterObj(preference))
----
>> Sequence based loop. iterate over a sequence
syntax 
---------
for varName in IterObj:
    block of code

Range 
---------
range(len) --> range(15) --> [0,1,2,3,4]
range(start, end) --> range(3,15) --> [3,4,5,...14]
range(start, end, step(1))

control statements of loops 
------------------------------
break --> stops the loop 
continue --> skip the current iteration

Data Structures 
------------------
>> Ordered Collection of data.

List 
-----
>> [v1,v2.....]
>> It allows duplicates
>> Indexed, Ordered
>> Mutable (Updatable)

In built methods 
-----------------
.append(ele)
.insert(pos,ele)
.extent(iterObj)

.remove(ele)
.pop(ind)
.clear()

.count(ele)
.index(ele)

.reverse()
.sort()

Tuple 
---------
>> (v1,v2.....)
>> Ordered, Indexed,
>> It allows duplicate 
>> Immutable

In built methods 
---------------------
.count(ele)
.index(ele)

Unpacking 
-------------
v1,v2,v3=tuple

Set 
------
>> {v1,v2...}
>> Mutable 
>> Unordered, Not indexed 
>> Does not allow duplicates.

In built methods 
------------------
.add(ele)
.remove(ele)

.union(s1)
.intersection(s1)
.difference(s1)

frozenset(set) (immutable)
.clear()

Dict 
------
>> {key:val,key:val.....}
>> Mutable 
>> Ordered, Not Indexed 
>> Allows Duplicate values. No Duplicate Keys

Inbuilt Methods 
------------------
.keys()
.values()
.items()

.pop(key)
.clear()

Functions 
--------------
>> Reusable block of code that performs a specific task.

In built Functions --> print, input......
User Defined Functions

Syntax:
-------
def fnName():
    block of code

Calling or Invoking --> fnName()

Parameters and Arguments 
-------------------------
p --> the variables you create in function defenition without value
a --> the values passes to the parameters.

Positional Arguments 
-----------------------
>> order matters.

Keyword Arguments
--------------------
>> pass arguments along with keywords.

Defualt Parameters
---------------------
>> Parameter with default value

Variable Length Arguments 
----------------------------
*arg --> Tuple
**karg --> Dict

return Keyword
------------------
>> it stops the function.
>> return a value to the caller.

Scopes 
---------
>> The region where a variable or name can be accessed.

Local Scope --> Variable created in a function. Accessible only within that func.

Global Scope --> Variable created outside of any function. Accessible anywhere.

Enclosing Scope --> variables of outer func are accessible in inner function.

Built In Scope --> for built in names. Ex: print, input, type..

parent(a)  --> child --> grandchild

File Handling 
----------------
>> It is the process of creating, reading, writing, deleting files using python.
Ex: txt, html, xml, xl, json, binary..

To open a file --> open(path, mode)
To read content --> .read()
To read content line by line --> .readline() .readlines()
To know the cursor pos --> .tell()
To change cursor pos --> .seek(pos)
To check readability --> .readable()
To check writability --> .writable()

To write content --> .write(content)

To close a file --> .close()
Modes 
-------
"r" --> Read
"w" --> Write
"a" --> append
"x" --> create
"r+" --> read and write (no truncation)
"w+" --> read and write 
"a+" --> read and append

with statement 
----------------
with open(path,mode) as varName:
    block of code 

Error Handling 
-----------------
>> Errors are the problems that stop the code from running correctly.

Syntax Errors 
----------------
>> when you make mistake in grammer of python.
Ex: SyntaxError, IndentationError

Runtime Errors (Exceptions)
--------------
>> these are caught during interpretation.
Ex: TypeError, ValueError, ZeroDivisionError....

Exception Handling
-------------------
try:
    block of code
except ErrName:
    handling block
.
.
finally:
    block of code

Logical Error

OOP 
----
Object Oriented Programming.
>> It is a programming pattern that organizes the code using class and object.

Object --> realtime entity.(information, funtionalities)
       --> (Attributes, Methods)

Blueprint --> Bottle,Bottle....
class(blueprint) --> obj1,obj2....(instances)

class 
------
>> It is a blueprint fpr the objects.
Syntax:
---------
class Clsname:
    define attr, method
    def __init__(self): #constructor
        attributes

Objects 
----------
>> Instances of a class.

Attrubutes 
-------------
Class Attribute --> accessed from both class and objects
InStance Attribute --> accessed only from the objects

Methods 
---------
Class Method --> when you want to use class attributes
Instance Method --> when you want to use instance attributes
Static Method

Self 
------
>> it is the current object we are working with.

Concepts of OOP 
------------------
Inheritance 
Poly 
Abstraction 
Encap.